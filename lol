

/**
/// ----> (x)
/// |
/// |
/// |
(y) ^


X `pixels` = if (X > 32) {
    round (X/32) * 2 `ascii char`
        } else
        1 `ascii char`
Y `pixels` = if (Y > 32) {
    round ( (Y/32) * 2 ) `ascii char`
} else
        1 `ascii char`
**
**/